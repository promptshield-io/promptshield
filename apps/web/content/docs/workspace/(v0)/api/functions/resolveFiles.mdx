[**@promptshield/workspace**](../index.md)

***

[@promptshield/workspace](../index.md) / resolveFiles

# Function: resolveFiles()

> **resolveFiles**(`patterns`, `root`): `Promise`\<`string`[]\>

Defined in: [resolve-files.ts:110](https://github.com/promptshield-io/promptshield/blob/main/packages/workspace/src/resolve-files.ts#L110)

Resolves workspace files using glob patterns while respecting
hierarchical ignore rules.

Behavior:
- Uses `fast-glob` for discovery.
- Applies ignore matcher after resolution.
- Returns absolute file paths.

## Parameters

### patterns

`string`[]

Glob patterns relative to workspace root.

### root

`string`

Absolute workspace root.

## Returns

`Promise`\<`string`[]\>

Absolute file paths eligible for scanning.
