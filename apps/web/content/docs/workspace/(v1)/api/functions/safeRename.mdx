[**@promptshield/workspace**](../index.md)

***

[@promptshield/workspace](../index.md) / safeRename

# Function: safeRename()

> **safeRename**(`from`, `to`): `Promise`\<`void`\>

Defined in: [utils.ts:188](https://github.com/promptshield-io/promptshield/blob/main/packages/workspace/src/utils.ts#L188)

Safely renames a file or directory.
- Skips if source does not exist
- Removes target if already exists

Prevents common Windows EPERM failures due to leftover folders.

## Parameters

### from

`string`

### to

`string`

## Returns

`Promise`\<`void`\>
