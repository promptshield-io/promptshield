[**@promptshield/cli**](../index.md)

***

[@promptshield/cli](../index.md) / runPromptShield

# Function: runPromptShield()

> **runPromptShield**(`options`): `Promise`\<`void`\>

Defined in: [main.ts:81](https://github.com/promptshield-io/promptshield/blob/main/packages/cli/src/main.ts#L81)

Executes PromptShield CLI workflow.

Processing flow:

sanitize:
  file → sanitize → write/preview

scan:
  file → scan → filter ignores → report

fix:
  file → scan → filter ignores → applyFixes → write/preview

check mode:
  stop scanning immediately after first detected threat
  and exit with non-zero status.

## Parameters

### options

[`PromptshieldCliOptions`](../interfaces/PromptshieldCliOptions.md)

## Returns

`Promise`\<`void`\>
